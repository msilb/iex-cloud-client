iex-cloud-client {
  environments {
    current = test
    test {
      base-url = dummy
      version = stable
      secret-key = Tsk_123
    }
    prod {
      base-url = dummy
      version = stable
      secret-key = sk_123
    }
  }
}
